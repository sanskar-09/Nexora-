specVersion: "v1"
serviceId: "nexora"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nexora-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
